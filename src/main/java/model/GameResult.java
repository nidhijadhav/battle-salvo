package model;

/**
 * The GameResult enum represents the possible outcomes of a game.
 * It can indicate whether the player has won, lost, or if the game ended in a draw.
 */
public enum GameResult {
  WIN,
  LOSE,
  DRAW
}
